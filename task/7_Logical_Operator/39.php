<!-- 39. NOT Operator -> Use the NOT (!) operator to check if a variable $isLoggedIn = false. Display
"Please Login" if not logged in.  -->
<?php
    $isLoggedIn = false;
    if($isLoggedIn)
        echo "You are logged in";
    else
        echo "Please login";
?>
